/**
 * Largura máxima do conteúdo das telas de lista/formulário — evita que o texto
 * estique de ponta a ponta em telas largas (desktop/tablet). Ver `breakpoints.ts`
 * pros pontos de corte do layout (sidebar etc.); este valor é sobre o conteúdo
 * em si, não sobre quando ele muda de layout.
 */
export const LARGURA_CONTEUDO = 720;
